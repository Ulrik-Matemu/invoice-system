rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    
    // Helper functions
    function isAuthenticated() {
      return request.auth != null;
    }
    
    function isOwner(userId) {
      return isAuthenticated() && request.auth.uid == userId;
    }

    // User Profiles
    match /users/{userId} {
      allow read: if isOwner(userId);
      // Allow users to create/update their profile (including isPro/invoiceCount for this demo)
      allow create, update: if isOwner(userId);
      allow delete: if isOwner(userId);
    }

    // Invoices
    match /invoices/{invoiceId} {
      allow read: if resource.data.userId == request.auth.uid;
      allow delete: if resource.data.userId == request.auth.uid;
      
      allow create: if isAuthenticated() 
        && request.resource.data.userId == request.auth.uid
        && (
          // Allow if user is Pro
          get(/databases/$(database)/documents/users/$(request.auth.uid)).data.isPro == true
          ||
          // OR if invoice count is less than allowed limit (default 5)
          get(/databases/$(database)/documents/users/$(request.auth.uid)).data.invoiceCount < get(/databases/$(database)/documents/users/$(request.auth.uid)).data.get('allowedInvoices', 5)
          ||
          // Handle case where user doc might not exist yet or count is missing (optional, but good for safety)
          !exists(/databases/$(database)/documents/users/$(request.auth.uid))
        );

      allow update: if resource.data.userId == request.auth.uid 
        && request.resource.data.userId == request.auth.uid
        && (
          // Allow if user is Pro
          get(/databases/$(database)/documents/users/$(request.auth.uid)).data.isPro == true
          ||
          // OR if they are NOT changing clientName or invoiceNumber
          (
            request.resource.data.clientName == resource.data.clientName
            && request.resource.data.invoiceNumber == resource.data.invoiceNumber
          )
        );
    }

    // Clients
    match /clients/{clientId} {
      allow read: if resource.data.userId == request.auth.uid;
      allow delete: if resource.data.userId == request.auth.uid;
      allow create: if isAuthenticated() && request.resource.data.userId == request.auth.uid;
      allow update: if resource.data.userId == request.auth.uid && request.resource.data.userId == request.auth.uid;
    }

    // Expenses
    match /expenses/{expenseId} {
      allow read, write, delete: if resource.data.userId == request.auth.uid || (isAuthenticated() && request.resource.data.userId == request.auth.uid);
    }

    // Settings
    match /settings/{userId} {
      allow read, write, delete: if isOwner(userId);
    }
  }
}
